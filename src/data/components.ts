import type { ComponentDoc } from '../types'

// The building blocks that recur across architectures. Ordered roughly by where
// they sit in a request's journey (client → edge → services → data → async).
export const componentDocs: ComponentDoc[] = [
  {
    id: 'client',
    name: 'Client',
    category: 'client',
    oneLiner: 'The user\'s device — where every request begins.',
    what: 'The end-user\'s device — a browser, mobile app, or another service — that originates requests and renders results. It is the one part of the system you do not control, so nothing it sends can be trusted: every input is validated server-side and secrets never live here. The client is also where latency is felt, so much design effort (CDNs, caching, pagination, optimistic UI) exists purely to make it feel fast. Because networks fail, clients retry — which is exactly why servers must be idempotent so a repeated request does not double-apply.',
    types: [
      { name: 'Web (browser SPA)', note: 'JavaScript app rendered in the browser.' },
      { name: 'Mobile', note: 'Native iOS / Android; flaky networks, aggressive retries.' },
      { name: 'API consumer', note: 'Another server or third party calling your API.' },
      { name: 'IoT / device', note: 'Constrained hardware, intermittent connectivity.' },
    ],
  },
  {
    id: 'dns',
    name: 'DNS',
    category: 'edge',
    oneLiner: 'The internet\'s phone book: hostname → IP address.',
    what: 'The Domain Name System translates a human hostname (example.com) into an IP address the client can connect to — effectively the internet\'s phone book, backed by a globally cached, hierarchical lookup. Beyond resolution, DNS is a traffic tool: latency- or geo-based routing hands each user the nearest region, and weighted/health-checked records shift traffic during deploys or outages. Crucially, DNS is a lookup, not a data hop — the client resolves a name, then connects directly to the returned address. Record TTLs trade propagation speed against caching efficiency.',
    types: [
      { name: 'Authoritative vs recursive', note: 'Who owns the record vs who looks it up for you.' },
      { name: 'GeoDNS / latency routing', note: 'Answer depends on where the user is.' },
      { name: 'Failover records', note: 'Health-checked; reroute on outage.' },
      { name: 'Anycast', note: 'One IP served from many locations.' },
    ],
  },
  {
    id: 'cdn',
    name: 'CDN',
    category: 'edge',
    oneLiner: 'Edge caches that serve content near the user.',
    what: 'A Content Delivery Network is a globally distributed set of edge caches that serve static or cacheable content (images, video, JS/CSS, sometimes API responses) from a location near the user. On a cache miss it fetches from your origin — often object storage — and caches the result. CDNs slash latency, absorb traffic spikes, and offload the majority of bytes from your servers, which then return only small dynamic payloads. The hard parts are cache invalidation (purging stale content on updates) and deciding what is cacheable. Modern CDNs also run edge compute and terminate TLS.',
    types: [
      { name: 'Static asset CDN', note: 'Images, JS/CSS, downloads.' },
      { name: 'Video / streaming', note: 'Segmented media, adaptive bitrate.' },
      { name: 'Edge compute', note: 'Run code at the edge (Cloudflare Workers).' },
      { name: 'Push vs pull', note: 'You upload vs CDN fetches from origin on demand.' },
    ],
  },
  {
    id: 'lb',
    name: 'Load Balancer',
    category: 'edge',
    oneLiner: 'Spreads traffic across many identical servers.',
    what: 'A load balancer spreads incoming traffic across many identical server instances, health-checks them, and removes dead nodes from rotation — the foundation of horizontal scaling and high availability. It also often terminates TLS and gives clients one stable entry point. Layer 4 balancers route on IP/port (fast, protocol-agnostic); Layer 7 balancers read HTTP and can route by path, header, or cookie, enabling canary releases and sticky sessions. Balancing strategies include round-robin, least-connections, and consistent hashing. The balancer itself must be redundant, or it becomes the single point of failure it was meant to prevent.',
    types: [
      { name: 'Layer 4 (TCP)', note: 'Fast, protocol-agnostic, routes on IP/port.' },
      { name: 'Layer 7 (HTTP)', note: 'Routes on path/header/cookie; canary, sticky.' },
      { name: 'Global vs regional', note: 'DNS/anycast across regions vs within one.' },
      { name: 'Algorithms', note: 'Round-robin, least-connections, consistent hashing.' },
    ],
  },
  {
    id: 'gateway',
    name: 'API Gateway',
    category: 'edge',
    oneLiner: 'The single front door to many backend services.',
    what: 'An API gateway is the single front door to a backend of many services. It handles cross-cutting concerns so individual services do not have to: TLS termination, authentication and authorization, rate limiting and quotas, request routing, and sometimes transformation, caching, or aggregation. In a microservice system it decouples clients from internal topology — the client calls one stable API while services evolve behind it. The risk is turning the gateway into a bloated bottleneck or single point of failure, so keep business logic out of it and run it redundantly.',
    types: [
      { name: 'Managed', note: 'AWS API Gateway, Apigee.' },
      { name: 'Self-hosted', note: 'Kong, Envoy, NGINX.' },
      { name: 'Backend-for-frontend', note: 'A tailored gateway per client type.' },
      { name: 'GraphQL gateway', note: 'One schema federating many services.' },
    ],
  },
  {
    id: 'service',
    name: 'Application Service',
    category: 'compute',
    oneLiner: 'Stateless business logic; scale by adding copies.',
    what: 'A stateless application (or micro-) service holds the business logic that processes a request: validating input, orchestrating data stores, and returning a result. "Stateless" is the key property — it keeps no client session in local memory, so any instance can handle any request and you scale simply by adding more identical copies behind a load balancer. Shared state lives in caches and databases instead. Services talk synchronously (HTTP/gRPC) or asynchronously (queues/events). Splitting a system into services buys independent scaling and deploys, at the cost of network calls, partial failure, and distributed-systems complexity.',
    types: [
      { name: 'Monolith', note: 'One deployable; simple until it is not.' },
      { name: 'Microservice', note: 'Independently deployed, scaled, owned.' },
      { name: 'Serverless function', note: 'Scale-to-zero, per-request billing.' },
      { name: 'Worker / consumer', note: 'Processes queue/stream jobs, not HTTP.' },
    ],
  },
  {
    id: 'auth',
    name: 'Auth Service',
    category: 'compute',
    oneLiner: 'Who you are (authn) and what you may do (authz).',
    what: 'The authentication/authorization service establishes who a caller is (authn) and what they may do (authz). It issues and validates credentials — typically short-lived signed tokens (JWT) or opaque session tokens stored server-side — so every downstream request is tied to a verified identity without re-checking a password. It centralizes login, token refresh, and permission checks, and often integrates SSO / OAuth / OIDC providers. Design tension: stateless JWTs scale well but are hard to revoke before expiry; server-side sessions revoke instantly but need a fast shared store. Never roll your own crypto.',
    types: [
      { name: 'Session-based', note: 'Server stores the session; easy to revoke.' },
      { name: 'Token-based (JWT)', note: 'Self-contained, stateless, hard to revoke early.' },
      { name: 'OAuth2 / OIDC', note: 'Delegated login via an identity provider.' },
      { name: 'API keys / mTLS', note: 'For service-to-service auth.' },
    ],
  },
  {
    id: 'cache',
    name: 'Cache',
    category: 'cache',
    oneLiner: 'Fast in-memory store on the hot read path.',
    what: 'A cache is a fast, usually in-memory store that holds frequently accessed data close to where it is read, so you avoid recomputing or re-fetching from a slower datastore. It absorbs read load and cuts latency from milliseconds to microseconds. Because cached data can go stale, the core decisions are the invalidation/expiry strategy (TTL, write-through, write-back, cache-aside) and what to do on a miss. Caches also hold ephemeral state like sessions, rate-limit counters, and holds/reservations. The classic trap is treating a best-effort cache as a source of truth for correctness.',
    types: [
      { name: 'In-memory KV', note: 'Redis, Memcached — shared across instances.' },
      { name: 'Local / in-process', note: 'Fastest, but per-instance and small.' },
      { name: 'Cache-aside', note: 'App reads cache, falls back to DB on miss.' },
      { name: 'Write-through / -back', note: 'Write hits cache then DB (or later).' },
    ],
  },
  {
    id: 'sqldb',
    name: 'Relational Database (SQL)',
    category: 'datastore',
    oneLiner: 'Tables, transactions, joins, strong consistency.',
    what: 'A relational database stores data in tables with enforced schemas and relationships, queried with SQL. Its superpowers are ACID transactions (all-or-nothing, consistent writes), joins across tables, and strong consistency — the default for money, inventory, orders, anything where correctness matters. Constraints (unique, foreign-key, check) let the database itself enforce invariants application code cannot bypass. The limit is write scaling: a single primary eventually bottlenecks, forcing read replicas and then sharding, which sacrifices the easy joins you adopted it for. Excellent until you outgrow one machine\'s write throughput.',
    types: [
      { name: 'PostgreSQL / MySQL', note: 'The workhorse single-node engines.' },
      { name: 'Primary + replicas', note: 'Scale reads; writes stay on the primary.' },
      { name: 'Sharded', note: 'Partition data across many primaries.' },
      { name: 'Distributed SQL', note: 'CockroachDB, Spanner — SQL that scales writes.' },
    ],
  },
  {
    id: 'nosql',
    name: 'NoSQL Store',
    category: 'datastore',
    oneLiner: 'Drops relational rules to scale horizontally.',
    what: 'NoSQL databases drop the relational model to gain horizontal scale and flexible schemas. Wide-column stores (Cassandra, DynamoDB) partition data by key across many nodes for massive write throughput and linear scaling — but no joins, tunable/eventual consistency, and you must design tables around your queries up front. Other families: document stores (MongoDB) for nested JSON, key-value (Redis) for simple lookups, and graph databases for relationship-heavy data. Rule of thumb: choose NoSQL when your access pattern is known, partitions cleanly, and needs scale beyond a single relational primary — and watch for hot partitions.',
    types: [
      { name: 'Wide-column', note: 'Cassandra, DynamoDB — huge write scale.' },
      { name: 'Document', note: 'MongoDB — nested JSON, flexible schema.' },
      { name: 'Key-value', note: 'Redis, Riak — simplest, fastest lookups.' },
      { name: 'Graph', note: 'Neo4j — relationship-heavy queries.' },
    ],
  },
  {
    id: 'replica',
    name: 'Read Replica',
    category: 'datastore',
    oneLiner: 'A read-only copy that scales reads off the primary.',
    what: 'A read replica is an asynchronously updated copy of a primary database that serves read queries, letting you scale reads independently of writes — essential for read-heavy systems. Writes go to the primary and stream to replicas with a small replication lag, so replicas are eventually consistent: a read right after a write may see stale data. You route heavy or non-critical reads (search, analytics, dashboards) to replicas, and keep read-after-write-sensitive reads on the primary. Replicas also serve as failover candidates for high availability. The permanent tradeoff is replication lag versus read scalability.',
    types: [
      { name: 'Async replication', note: 'Fast, but replicas lag the primary.' },
      { name: 'Semi-sync', note: 'Wait for one replica to ack; safer, slower.' },
      { name: 'Cross-region', note: 'Reads near users; larger lag.' },
      { name: 'Failover standby', note: 'Promoted to primary on failure.' },
    ],
  },
  {
    id: 'objectstore',
    name: 'Object Storage',
    category: 'datastore',
    oneLiner: 'Cheap, near-infinite storage for large blobs.',
    what: 'Object storage (S3, GCS) stores large, unstructured blobs — images, video, backups, logs, documents — as objects in a flat namespace, addressed by key. It is effectively infinite, cheap, and highly durable (data replicated across facilities), but it is not a database: no queries, no transactions, and higher latency than a disk or cache. You store the blob and keep a URL/reference in your database. It is the standard origin behind a CDN and the sink for data lakes and backups. Access is via HTTP APIs, with lifecycle rules to tier or expire old data.',
    types: [
      { name: 'Storage tiers', note: 'Standard → infrequent-access → archive (cost vs speed).' },
      { name: 'Versioned buckets', note: 'Keep old versions; recover from deletes.' },
      { name: 'Presigned URLs', note: 'Time-limited direct client upload/download.' },
      { name: 'Static hosting', note: 'Serve a website straight from a bucket.' },
    ],
  },
  {
    id: 'searchindex',
    name: 'Search Index',
    category: 'datastore',
    oneLiner: 'Inverted index for fast full-text search + ranking.',
    what: 'A search index (Elasticsearch, OpenSearch, Solr) is a read-optimized store built on an inverted index — a map from each term to the documents containing it — enabling fast full-text search, relevance ranking, typo tolerance, and faceted filtering that SQL LIKE cannot match. It is a secondary index, not a source of truth: you sync it from your primary database via a pipeline (change-data-capture or batch), so it is eventually consistent. Use it for product catalogs, log search, and autocomplete. The cost is an extra system to operate and keep in sync, plus index storage overhead.',
    types: [
      { name: 'Inverted index', note: 'Elasticsearch, Solr — classic full-text.' },
      { name: 'Vector / semantic', note: 'Embeddings for meaning-based search.' },
      { name: 'Managed', note: 'Algolia — search-as-a-service.' },
      { name: 'DB-native', note: 'Postgres tsvector for modest needs.' },
    ],
  },
  {
    id: 'queue',
    name: 'Message Queue',
    category: 'queue',
    oneLiner: 'Buffers work so it runs async off the request path.',
    what: 'A message queue decouples a producer from a consumer by buffering work in a durable middle layer, so slow, spiky, or failure-prone tasks run asynchronously off the request path. The producer enqueues and returns immediately; workers pull and process at their own pace, with retries and a dead-letter queue for poison messages. This smooths load spikes and isolates failures, at the cost of eventual consistency and a need for idempotent consumers — queues deliver at-least-once, so duplicates happen. Classic uses: sending email, processing orders, fan-out. A queue typically deletes a message once it is consumed.',
    types: [
      { name: 'Broker', note: 'RabbitMQ, SQS — classic point-to-point queue.' },
      { name: 'Pub/sub', note: 'One message fanned out to many subscribers.' },
      { name: 'FIFO vs standard', note: 'Strict order (slower) vs best-effort (faster).' },
      { name: 'Dead-letter queue', note: 'Where messages go after repeated failures.' },
    ],
  },
  {
    id: 'eventlog',
    name: 'Event Log / Stream',
    category: 'queue',
    oneLiner: 'Durable, ordered, replayable log of events.',
    what: 'An event log (Kafka, Kinesis, Pulsar) is a durable, ordered, append-only record of events that multiple consumers read independently at their own offset. Unlike a queue — which usually deletes a message after one consumer handles it — the log retains events, so you can replay history to rebuild state, add new consumers, or recover after a bug. This makes it the backbone of event sourcing, stream processing, and change-data-capture. Ordering is guaranteed within a partition, and throughput scales by partitioning. The tradeoffs are operational complexity and reasoning carefully about consumer lag and offsets.',
    types: [
      { name: 'Log / stream', note: 'Kafka, Pulsar, Kinesis.' },
      { name: 'Partitioned', note: 'Order within a partition; scale across them.' },
      { name: 'Consumer groups', note: 'Many independent readers, own offsets.' },
      { name: 'CDC pipeline', note: 'Stream a database\'s changes as events.' },
    ],
  },
  {
    id: 'realtime',
    name: 'Real-time Gateway',
    category: 'edge',
    oneLiner: 'Long-lived connections that push updates to clients.',
    what: 'A real-time gateway maintains long-lived, bidirectional connections (WebSocket, Server-Sent Events, or long-polling) so the server can push data to clients the instant it changes — for live feeds, chat, dashboards, and market/odds updates — instead of clients polling. Because connections are stateful and long-lived, the gateway must track which client subscribes to what and scale horizontally with a pub/sub fan-out layer (often Redis) broadcasting updates across nodes. Challenges: connection state, reconnection and resync after drops, and backpressure. Push beats polling on freshness and wasted requests, but costs stateful-connection management.',
    types: [
      { name: 'WebSocket', note: 'Full-duplex; the general-purpose choice.' },
      { name: 'Server-Sent Events', note: 'Server→client only; simpler than WS.' },
      { name: 'Long-polling', note: 'Fallback where WS is unavailable.' },
      { name: 'Managed', note: 'Pusher, Ably — real-time as a service.' },
    ],
  },
  {
    id: 'observability',
    name: 'Observability',
    category: 'compute',
    oneLiner: 'Metrics, logs, traces — how you see the system run.',
    what: 'Observability is how you understand a running system from the outside, via three pillars: metrics (numeric time-series like latency, QPS, error rate), logs (structured event records), and traces (the path of one request across services). Together with dashboards and alerting, they let you detect, diagnose, and often predict failures before users complain — indispensable for events you cannot rehearse, like a flash sale. Good observability turns "the site is slow" into "this service\'s p99 spiked because that database is saturated." At scale it is not optional infrastructure; it is how you operate safely.',
    types: [
      { name: 'Metrics', note: 'Prometheus, StatsD — time-series + alerts.' },
      { name: 'Logs', note: 'ELK, Loki — searchable event records.' },
      { name: 'Traces', note: 'Jaeger, OpenTelemetry — request across services.' },
      { name: 'APM suites', note: 'Datadog, New Relic — all three, integrated.' },
    ],
  },
]
