import type { ComponentDoc } from '../types'

// The building blocks that recur across architectures. Ordered roughly by where
// they sit in a request's journey (client → edge → services → data → async).
export const componentDocs: ComponentDoc[] = [
  {
    id: 'client',
    name: 'Client',
    category: 'client',
    oneLiner: 'The user\'s device — where every request starts.',
    what: 'The client is the user\'s device — a browser, a mobile app, or another program — that sends requests and shows the results. It is the one part of the system you do not control, so you cannot trust anything it sends: the server always re-checks every input, and secrets are never stored here. The client is also where slowness is felt, so a lot of design work (CDNs, caching, loading data in pages, showing results before they are confirmed) exists just to make it feel fast. Networks fail, so clients retry — which is why servers must be idempotent (safe to receive the same request twice without doing it twice).',
    types: [
      { name: 'Web (browser SPA)', note: 'A JavaScript app that runs in the browser.' },
      { name: 'Mobile', note: 'Native iOS / Android; shaky networks, lots of retries.' },
      { name: 'API consumer', note: 'Another server or outside company calling your API.' },
      { name: 'IoT / device', note: 'Low-power hardware that connects on and off.' },
    ],
  },
  {
    id: 'dns',
    name: 'DNS',
    category: 'edge',
    oneLiner: 'The internet\'s phone book: hostname → IP address.',
    what: 'DNS (the Domain Name System) turns a name people can read (example.com) into an IP address the client can connect to — it is basically the internet\'s phone book, and its answers are cached all over the world. DNS is also a traffic tool: it can send each user to the nearest region, and it can shift traffic during deploys or outages using records that are weighted or health-checked. One key point: DNS is a lookup, not a traffic hop — the client looks up the name, then connects directly to the address it got back. Each record has a TTL (how long it may be cached): a short TTL means changes spread faster, a long TTL means less lookup traffic.',
    types: [
      { name: 'Authoritative vs recursive', note: 'Who owns the record vs who does the lookup for you.' },
      { name: 'GeoDNS / latency routing', note: 'The answer depends on where the user is.' },
      { name: 'Failover records', note: 'Health-checked; reroute traffic during an outage.' },
      { name: 'Anycast', note: 'One IP address served from many locations.' },
    ],
  },
  {
    id: 'cdn',
    name: 'CDN',
    category: 'edge',
    oneLiner: 'Caches near the user that serve content fast.',
    what: 'A CDN (Content Delivery Network) is a set of caches spread around the world that serve copies of your content — images, video, JS/CSS, sometimes API responses — from a location close to the user. If a cache does not have the content yet (a cache miss), it fetches it from your origin (often object storage) and saves a copy for next time. CDNs cut latency, soak up traffic spikes, and take most of the data load off your servers, which then only send small, changing bits. The tricky parts are cache invalidation (clearing out old content when things change) and deciding what is safe to cache. Modern CDNs can also run code at the edge and handle TLS (the encryption for HTTPS).',
    types: [
      { name: 'Static asset CDN', note: 'Images, JS/CSS, downloads.' },
      { name: 'Video / streaming', note: 'Media split into chunks; quality adapts to your connection.' },
      { name: 'Edge compute', note: 'Run code at the edge, near users (Cloudflare Workers).' },
      { name: 'Push vs pull', note: 'You upload content vs the CDN fetches it from the origin when first asked.' },
    ],
  },
  {
    id: 'lb',
    name: 'Load Balancer',
    category: 'edge',
    oneLiner: 'Spreads traffic across many identical servers.',
    what: 'A load balancer spreads incoming traffic across many identical copies of a server, checks that each one is healthy, and stops sending traffic to any that die. This is the basis of horizontal scaling (adding more servers) and high availability (staying up when some fail). It often handles TLS (the HTTPS encryption) and gives clients one stable address to reach. A Layer 4 balancer routes by IP address and port (fast, and it does not care what the traffic is); a Layer 7 balancer reads the HTTP request and can route by path, header, or cookie, which enables canary releases (sending a little traffic to a new version) and sticky sessions (keeping a user on the same server). Common strategies: round-robin, least-connections, and consistent hashing. The balancer itself must have a backup, or it becomes the single point of failure it was meant to prevent.',
    types: [
      { name: 'Layer 4 (TCP)', note: 'Fast; routes by IP/port and ignores what the traffic is.' },
      { name: 'Layer 7 (HTTP)', note: 'Routes by path/header/cookie; enables canary and sticky sessions.' },
      { name: 'Global vs regional', note: 'Balancing across regions vs within one.' },
      { name: 'Algorithms', note: 'Round-robin, least-connections, consistent hashing.' },
    ],
  },
  {
    id: 'gateway',
    name: 'API Gateway',
    category: 'edge',
    oneLiner: 'The single front door to many backend services.',
    what: 'An API gateway is the single front door to a backend made of many services. It handles the jobs that every service would otherwise repeat: TLS (HTTPS encryption), checking who the caller is and what they may do, rate limiting and quotas (capping how often someone can call), routing requests to the right service, and sometimes reshaping, caching, or combining responses. In a microservice system it hides the internal layout from clients — the client calls one stable API while the services behind it change freely. The danger is letting the gateway grow into a bloated bottleneck or a single point of failure, so keep business logic out of it and run more than one copy.',
    types: [
      { name: 'Managed', note: 'Run for you by a cloud provider (AWS API Gateway, Apigee).' },
      { name: 'Self-hosted', note: 'You run it yourself (Kong, Envoy, NGINX).' },
      { name: 'Backend-for-frontend', note: 'A separate gateway tailored to each type of client.' },
      { name: 'GraphQL gateway', note: 'One schema that stitches together many services.' },
    ],
  },
  {
    id: 'service',
    name: 'Application Service',
    category: 'compute',
    oneLiner: 'Stateless business logic; scale by adding copies.',
    what: 'An application service (sometimes a microservice) holds the business logic that handles a request: it checks the input, reads and writes data stores, and returns a result. The key trait is that it is stateless — it keeps nothing about a user\'s session in its own memory, so any copy can handle any request and you scale simply by running more identical copies behind a load balancer. Shared state lives in caches and databases instead. Services talk to each other either synchronously (waiting for a reply, over HTTP/gRPC) or asynchronously (via queues/events). Splitting a system into services lets you scale and deploy each one on its own, but you pay for it with extra network calls, partial failures, and the general difficulty of distributed systems.',
    types: [
      { name: 'Monolith', note: 'One app you deploy as a whole; simple until it is not.' },
      { name: 'Microservice', note: 'Deployed, scaled, and owned on its own.' },
      { name: 'Serverless function', note: 'Scales down to zero; you pay per request.' },
      { name: 'Worker / consumer', note: 'Processes jobs from a queue/stream, not HTTP requests.' },
    ],
  },
  {
    id: 'auth',
    name: 'Auth Service',
    category: 'compute',
    oneLiner: 'Who you are (authn) and what you may do (authz).',
    what: 'The auth service figures out who a caller is (authentication, or authn) and what they are allowed to do (authorization, or authz). It hands out and checks credentials — usually short-lived signed tokens (JWTs) or opaque session tokens kept on the server — so every later request is tied to a verified identity without asking for the password again. It puts login, token refresh, and permission checks in one place, and often connects to SSO / OAuth / OIDC providers (services that let users log in with an existing account). The tradeoff: stateless JWTs scale well but are hard to cancel before they expire; server-side sessions can be canceled instantly but need a fast shared store to look them up. Never write your own crypto.',
    types: [
      { name: 'Session-based', note: 'Server stores the session; easy to cancel.' },
      { name: 'Token-based (JWT)', note: 'Self-contained, stateless, hard to cancel before it expires.' },
      { name: 'OAuth2 / OIDC', note: 'Log in through an outside identity provider.' },
      { name: 'API keys / mTLS', note: 'For one service proving its identity to another.' },
    ],
  },
  {
    id: 'cache',
    name: 'Cache',
    category: 'cache',
    oneLiner: 'Fast in-memory store for frequent reads.',
    what: 'A cache is a fast store, usually kept in memory, that holds copies of frequently used data close to where it is read, so you avoid recomputing it or fetching it again from a slower store. It takes load off reads and cuts latency from milliseconds down to microseconds. Because a copy can go stale (out of date), the main decisions are how you expire or refresh it (TTL, write-through, write-back, cache-aside) and what to do on a miss (when the data is not there). Caches also hold short-lived data like sessions, rate-limit counters, and holds/reservations. The classic mistake is trusting a best-effort cache as a source of truth for correctness — it is not one.',
    types: [
      { name: 'In-memory KV', note: 'Redis, Memcached — shared by all your servers.' },
      { name: 'Local / in-process', note: 'Fastest, but lives in one server and stays small.' },
      { name: 'Cache-aside', note: 'App checks the cache first, falls back to the DB on a miss.' },
      { name: 'Write-through / -back', note: 'Writes go to the cache, then to the DB (now or later).' },
    ],
  },
  {
    id: 'sqldb',
    name: 'Relational Database (SQL)',
    category: 'datastore',
    oneLiner: 'Tables, transactions, joins, strong consistency.',
    what: 'A relational database (SQL) stores data in tables with a fixed schema (defined columns and types) and links between them, and you query it with SQL. Its strengths are transactions that are ACID (a group of writes either all happen or none do, and the data stays consistent), joins that combine related tables, and strong consistency (a read always sees the latest write) — the default choice for money, inventory, orders, anything where being exactly right matters. Constraints (unique, foreign-key, check) let the database enforce rules that application code cannot skip. The limit is write scaling: one primary server eventually hits a ceiling, pushing you to read replicas and then sharding (splitting data across servers), which gives up the easy joins you chose it for. Excellent until you outgrow a single machine\'s write capacity.',
    types: [
      { name: 'PostgreSQL / MySQL', note: 'The go-to engines that run on a single server.' },
      { name: 'Primary + replicas', note: 'Copies scale reads; writes still go to the one primary.' },
      { name: 'Sharded', note: 'Split the data across many primary servers.' },
      { name: 'Distributed SQL', note: 'CockroachDB, Spanner — SQL that also scales writes.' },
    ],
  },
  {
    id: 'nosql',
    name: 'NoSQL Store',
    category: 'datastore',
    oneLiner: 'Drops relational rules to scale across servers.',
    what: 'NoSQL databases give up the relational model to gain easy scaling across many servers and flexible schemas (you do not have to fix the columns up front). Wide-column stores (Cassandra, DynamoDB) split data by key across many servers for huge write throughput that grows as you add servers — but there are no joins, consistency is tunable and often eventual (reads may briefly see old data), and you must design your tables around the queries you will run before you start. Other families: document stores (MongoDB) for nested JSON, key-value (Redis) for simple lookups, and graph databases for data that is all about relationships. Rule of thumb: pick NoSQL when you know your access pattern, the data splits cleanly by key, and you need more scale than a single relational primary can give — and watch out for hot partitions (one key getting far more traffic than the rest).',
    types: [
      { name: 'Wide-column', note: 'Cassandra, DynamoDB — huge write scale.' },
      { name: 'Document', note: 'MongoDB — nested JSON, no fixed schema.' },
      { name: 'Key-value', note: 'Redis, Riak — the simplest, fastest lookups.' },
      { name: 'Graph', note: 'Neo4j — queries that follow relationships.' },
    ],
  },
  {
    id: 'replica',
    name: 'Read Replica',
    category: 'datastore',
    oneLiner: 'A read-only copy that scales reads off the primary.',
    what: 'A read replica is a copy of the primary database, kept up to date in the background, that answers read queries — letting you scale reads separately from writes, which read-heavy systems need. Writes go to the primary and then stream to the replicas with a small replication lag (a short delay), so replicas are eventually consistent: a read right after a write may still see old data. You send heavy or non-urgent reads (search, analytics, dashboards) to replicas, and keep reads that must see your own latest write on the primary. Replicas can also be promoted to take over if the primary fails, which helps availability. The lasting tradeoff is replication lag versus how much you can scale reads.',
    types: [
      { name: 'Async replication', note: 'Fast, but replicas trail the primary.' },
      { name: 'Semi-sync', note: 'Wait for one replica to confirm; safer, slower.' },
      { name: 'Cross-region', note: 'Reads close to users, but larger lag.' },
      { name: 'Failover standby', note: 'Promoted to primary if the primary fails.' },
    ],
  },
  {
    id: 'objectstore',
    name: 'Object Storage',
    category: 'datastore',
    oneLiner: 'Cheap, near-endless storage for large files.',
    what: 'Object storage (S3, GCS) holds large files (blobs) — images, video, backups, logs, documents — as objects you look up by a key, with no folder tree, just one flat namespace. It is effectively unlimited, cheap, and very durable (each file is copied across multiple facilities), but it is not a database: no queries, no transactions, and slower than a local disk or a cache. You store the file here and keep its URL or reference in your database. It is the usual origin behind a CDN and the place data lakes and backups get dumped. You use it over HTTP APIs, with lifecycle rules to move old data to cheaper tiers or delete it automatically.',
    types: [
      { name: 'Storage tiers', note: 'Standard → infrequent-access → archive (cheaper but slower).' },
      { name: 'Versioned buckets', note: 'Keep old versions; recover from accidental deletes.' },
      { name: 'Presigned URLs', note: 'Time-limited links that let a client upload/download directly.' },
      { name: 'Static hosting', note: 'Serve a website straight from a bucket.' },
    ],
  },
  {
    id: 'searchindex',
    name: 'Search Index',
    category: 'datastore',
    oneLiner: 'Inverted index for fast full-text search + ranking.',
    what: 'A search index (Elasticsearch, OpenSearch, Solr) is a store tuned for reading, built on an inverted index — a map from each word to the documents that contain it. That gives you fast full-text search, ranking by relevance, tolerance for typos, and filtering by facets, which a plain SQL LIKE query cannot match. It is a secondary copy, not a source of truth: you feed it from your primary database through a pipeline (change-data-capture, which streams every change, or periodic batches), so it is eventually consistent (it may briefly lag the database). Use it for product catalogs, log search, and autocomplete. The cost is another system to run and keep in sync, plus the extra storage the index takes.',
    types: [
      { name: 'Inverted index', note: 'Elasticsearch, Solr — classic full-text search.' },
      { name: 'Vector / semantic', note: 'Searches by meaning, not exact words (uses embeddings).' },
      { name: 'Managed', note: 'Algolia — search run for you as a service.' },
      { name: 'DB-native', note: 'Postgres tsvector, good enough for modest needs.' },
    ],
  },
  {
    id: 'queue',
    name: 'Message Queue',
    category: 'queue',
    oneLiner: 'Buffers work so it runs later, off the request path.',
    what: 'A message queue sits between a producer (who creates work) and a consumer (who does it), holding the work safely in the middle so slow, spiky, or failure-prone tasks can run later instead of during the request. The producer drops a message on the queue and returns right away; workers pull messages and process them at their own pace, retrying failures and sending hopeless ones to a dead-letter queue. This smooths out traffic spikes and keeps failures contained, at the cost of eventual consistency and a need for idempotent consumers (safe to process the same message twice) — a queue delivers at-least-once, so the same message can arrive more than once. Common uses: sending email, processing orders, fan-out. A queue usually deletes a message once it has been consumed.',
    types: [
      { name: 'Broker', note: 'RabbitMQ, SQS — classic one-sender-to-one-receiver queue.' },
      { name: 'Pub/sub', note: 'One message copied out to many subscribers.' },
      { name: 'FIFO vs standard', note: 'Strict order (slower) vs best-effort order (faster).' },
      { name: 'Dead-letter queue', note: 'Where messages land after repeated failures.' },
    ],
  },
  {
    id: 'eventlog',
    name: 'Event Log / Stream',
    category: 'queue',
    oneLiner: 'Durable, ordered, replayable log of events.',
    what: 'An event log (Kafka, Kinesis, Pulsar) is a durable, ordered record that you only ever add to. Many consumers read it independently, each tracking its own position (offset). Unlike a queue — which usually deletes a message after one consumer handles it — the log keeps its events, so you can replay history to rebuild state, plug in a new consumer, or recover after a bug. That is what makes it the backbone of event sourcing, stream processing, and change-data-capture. Order is guaranteed within a partition (a slice of the log), and you get more throughput by adding partitions. The tradeoffs: it is more work to run, and you have to think carefully about consumer lag (how far behind a reader is) and offsets.',
    types: [
      { name: 'Log / stream', note: 'Kafka, Pulsar, Kinesis.' },
      { name: 'Partitioned', note: 'Order within a partition; add partitions to scale.' },
      { name: 'Consumer groups', note: 'Many independent readers, each with its own position.' },
      { name: 'CDC pipeline', note: 'Stream every change in a database out as events.' },
    ],
  },
  {
    id: 'realtime',
    name: 'Real-time Gateway',
    category: 'edge',
    oneLiner: 'Long-lived connections that push updates to clients.',
    what: 'A real-time gateway holds open long-lived, two-way connections (WebSocket, Server-Sent Events, or long-polling) so the server can push data to clients the moment it changes — for live feeds, chat, dashboards, and market/odds updates — instead of clients asking again and again (polling). Because each connection stays open and carries state, the gateway has to track which client is subscribed to what, and to scale across many servers it uses a pub/sub fan-out layer (often Redis) that broadcasts updates to all of them. The hard parts: managing all that connection state, reconnecting and resyncing after a drop, and backpressure (slowing down when a client cannot keep up). Pushing wins on freshness and avoids wasted requests, but you pay for it by managing all those open connections.',
    types: [
      { name: 'WebSocket', note: 'Two-way; the general-purpose choice.' },
      { name: 'Server-Sent Events', note: 'Server-to-client only; simpler than WebSocket.' },
      { name: 'Long-polling', note: 'Fallback for when WebSocket is not available.' },
      { name: 'Managed', note: 'Pusher, Ably — real-time run for you as a service.' },
    ],
  },
  {
    id: 'observability',
    name: 'Observability',
    category: 'compute',
    oneLiner: 'Metrics, logs, traces — how you see the system run.',
    what: 'Observability is how you understand a running system from the outside, and it rests on three pillars: metrics (numbers tracked over time, like latency, requests per second, and error rate), logs (a structured record of things that happened), and traces (the full path of one request as it moves across services). With dashboards and alerts on top, they let you spot, diagnose, and often predict failures before users complain — which matters most for moments you cannot rehearse, like a flash sale. Good observability turns "the site is slow" into "this service\'s p99 (its slowest 1% of requests) spiked because that database is overloaded." At scale it is not optional; it is how you run the system safely.',
    types: [
      { name: 'Metrics', note: 'Prometheus, StatsD — numbers over time, plus alerts.' },
      { name: 'Logs', note: 'ELK, Loki — searchable records of what happened.' },
      { name: 'Traces', note: 'Jaeger, OpenTelemetry — one request across services.' },
      { name: 'APM suites', note: 'Datadog, New Relic — all three, in one tool.' },
    ],
  },
]
